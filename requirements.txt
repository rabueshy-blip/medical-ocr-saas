# --- PDF رقمي: نص + جداول (بديل بايثون خالص، بدون ghostscript) ---
pymupdf
pdfplumber

# --- OCR للصفحات الممسوحة ---
# ملاحظة: paddlepaddle/paddleocr (محرك التحقق المزدوج المخطَّط له في plan.md القسم 3-ب)
# أُزيلا عمداً من هنا لأنهما غير مستوردَين فعلياً في أي كود حالياً وثقيلان جداً لبناء
# سريع على Streamlit Community Cloud (حصة موارد محدودة) — يُعادان عند بناء التحقق المزدوج فعلياً.
easyocr

# --- مقارنة/تحقق مزدوج + ربط قاموس المصطلحات ---
rapidfuzz

# --- DSPy وموديولات التصحيح ---
dspy-ai

# --- Vision LLM: Gemini عبر Google AI Studio (مجاني)، يُستدعى عبر litellm
# (اعتمادية ضمن dspy-ai مباشرةً) بصيغة "gemini/<model>" — لا حاجة لحزمة SDK إضافية ---
python-dotenv

# --- واجهة المستخدم ---
streamlit

# --- FastAPI: ربط الـ pipeline المُحسَّن بنقاط API خارجية ---
fastapi
uvicorn[standard]

# --- اعتماديات مساعدة ---
opencv-python-headless
numpy
pandas
Pillow
